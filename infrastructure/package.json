{
  "name": "bamf-fragenkatalog",
  "version": "1.0.0",
  "description": "Capture BAMF Leben in Deutschland / Einbürgerungstest questions to JSON",
  "type": "module",
  "scripts": {
    "capture": "node capture-questions.js",
    "capture:resume": "node capture-questions.js --resume",
    "capture:from-start": "node capture-questions.js --from=1",
    "test": "node capture-questions.js --from=1 --test"
  },
  "dependencies": {
    "playwright": "^1.49.0",
    "tesseract.js": "^6.0.0"
  }
}
